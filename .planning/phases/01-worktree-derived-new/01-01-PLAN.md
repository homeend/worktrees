---
phase: 01-worktree-derived-new
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/worktree/types.go
  - pkg/worktree/manager.go
  - pkg/worktree/manager_test.go
  - pkg/worktree/fakes_test.go
  - cmd/wt/new.go
  - cmd/wt/new_test.go
  - pkg/worktree/manager_integration_test.go
autonomous: true
requirements:
  - DETECT-01
  - DETECT-02
  - DERIVE-01
  - DERIVE-02
  - DERIVE-03
  - NAME-01
  - NAME-02
  - NAME-03
  - NAME-04
user_setup: []

must_haves:
  truths:
    - "Running `wt new` with no token from inside a worktree creates branch+worktree `<current-branch>-v001`, branched off the committed tip of the current branch, in the main repo's container (DETECT-01, DERIVE-01, DERIVE-02, NAME-01)"
    - "Running `wt new` repeatedly picks the lowest free `-vNNN` ≥ 1, skipping existing branches and filling gaps (NAME-02)"
    - "Running `wt new -- \"-patch01\"` inside a worktree creates `<current-branch>-patch01`; if that branch exists it fails with a clear collision error, no rename/auto-bump (NAME-03, NAME-04)"
    - "A derived branch keeps the parent branch's prefix verbatim; `--no-prefix`/`--branch-prefix` in derive mode do not alter the inherited prefix (DERIVE-03)"
    - "Running `wt new` from the main repo root branches off base_ref/HEAD exactly as today, unchanged naming and placement (DETECT-02)"
  artifacts:
    - path: "pkg/worktree/manager.go"
      provides: "Derive-mode detection + name/branch/baseRef resolution inside Manager.Add"
      contains: "currentWorktreeBranch"
    - path: "cmd/wt/new.go"
      provides: "Positional token passthrough (literal suffix) without breaking main-root semantics"
    - path: "pkg/worktree/manager_integration_test.go"
      provides: "Real-git end-to-end coverage of all 5 success criteria"
  key_links:
    - from: "pkg/worktree/manager.go Add"
      to: "GitRunner.ListWorktrees"
      via: "match the listed worktree whose Path is the longest prefix of dir to detect derive mode and read its Branch"
      pattern: "ListWorktrees"
    - from: "pkg/worktree/manager.go Add"
      to: "GitRunner.BranchExists"
      via: "lowest-free -vNNN search and custom-token collision check against repoRoot"
      pattern: "BranchExists"
---

<objective>
Make `wt new` derive the new branch from the current worktree's branch when run from inside a managed (non-main) worktree, auto-named `<current-branch>-vNNN` or with a caller-supplied literal suffix token — while main-repo-root behavior stays exactly as today. This is one tightly-coupled vertical slice through `Manager.Add` plus the CLI positional-arg wiring, landed with unit + integration tests.

Purpose: Zero-friction sibling iteration of the worktree you're standing in, without returning to the repo root or naming the branch by hand.
Output: Derive-mode logic in `Manager.Add`, CLI token passthrough in `cmd/wt/new.go`, extended fakes, unit tests, and an end-to-end integration test covering all five ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@pkg/worktree/manager.go
@pkg/worktree/types.go
@pkg/worktree/interfaces.go
@cmd/wt/new.go

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. Use directly — no exploration required. -->

GitRunner (pkg/worktree/interfaces.go) — already sufficient; NO new method required:
  MainRoot(dir string) (string, error)            // MAIN working-tree root (parent of --git-common-dir)
  VerifyRef(dir, ref string) error
  CheckRefFormat(branch string) error
  BranchExists(dir, branch string) bool           // refs/heads/<branch> exists (short name, no refs/heads/ prefix)
  AddWorktree(dir, path, branch, base string) error
  ListWorktrees(dir string) ([]GitWorktree, error)

GitWorktree (pkg/worktree/interfaces.go):
  Path string; Branch string  // Branch is "refs/heads/<name>", empty if detached/bare

AddOptions (pkg/worktree/types.go) — TODAY:
  Name, Branch, BaseRef string; NoHooks, NoPrefix bool; FromBranch, PrefixOverride string
  NEW FIELD this plan adds: FromTemplate bool  // true when Name was produced by --template (so derive mode can defer to today's behavior)

Manager.Add (pkg/worktree/manager.go) today:
  - resolves repoRoot := MainRoot(dir)
  - fromExisting fork (opts.FromBranch != "") OR resolveNames(opts)
  - CheckRefFormat(branch) gate, BranchExists collision gate, VerifyRef(baseRef)
  - target := worktreePath(repoRoot, branch)  // mirrors full prefixed branch into container; REUSE unchanged for DERIVE-02
  - AddWorktree(repoRoot, target, branch, baseRef)

worktreePath(repoRoot, branch) (manager.go) — already mirrors full branch into container; reuse for DERIVE-02.

CLI fake (cmd/wt/new_test.go) drives buildAddOptions(r addResolver, args, tmpl, fromBranch, branch, base, noHooks, noPrefix, prefixOverride).
buildAddOptions writes opts.Name from BOTH the positional default (args[0]) AND the --template case (ResolveTemplate result) — these are indistinguishable downstream by Name alone; FromTemplate disambiguates.
</interfaces>
</context>

<design_constraints>
<!-- Locked decisions and verified facts. Honor exactly. -->

- DERIVE-MODE TRIGGER PREDICATE (gate for DETECT-01/02 + regression safety): activate derive mode ONLY when ALL hold:
  (a) the listed worktree containing `dir` is NOT the main worktree (its Path != MainRoot(dir)), AND
  (b) the invocation supplies no explicit branch-source: opts.Branch == "" AND opts.FromBranch == "" AND opts.FromTemplate == false.
  Rationale for FromTemplate: both a positional suffix token and a `--template`-rendered name land in opts.Name, so Name alone cannot distinguish them. A `--template` invocation must fall through to today's behavior (like --branch/--from-branch), NOT be reinterpreted as a suffix token. The CLI sets opts.FromTemplate=true in the template case.
  If any condition fails → fall through to TODAY'S behavior unchanged (DETECT-02). Detection uses the existing ListWorktrees; pick the entry whose Path is the longest path-separator-bounded prefix of `dir` (cwd may be a subdir of the worktree). The container is a SIBLING of repoRoot, so no false ancestor match against main.
- DERIVE-01 (committed tip): set baseRef to the parent branch NAME (e.g. `wt/feature-login`). `git worktree add -b <new> <path> <parentBranch>` cuts from that ref's commit — uncommitted changes are excluded by construction. No copy machinery.
- DERIVE-02 (placement): keep targeting MainRoot(dir) and reuse worktreePath unchanged. New worktree lands in the main container, mirroring the full branch path. Do NOT nest under the current worktree.
- DERIVE-03 (prefix verbatim): in derive mode the new branch = parentBranch + suffix; parentBranch already carries its prefix. Do NOT call effectivePrefix / apply NoPrefix / PrefixOverride / cfg.BranchPrefix() in derive mode. Also: `--base` is IGNORED in derive mode (baseRef is the parent branch).
- NAME-01 / NAME-02 (auto suffix): when token is empty, branch := first free `fmt.Sprintf("%s-v%03d", parentBranch, n)` for n starting at 1, increment while BranchExists(repoRoot, candidate) — lowest free, gap-filling, zero-padded width 3.
- NAME-03 (custom token): token is the literal positional arg (e.g. `-patch01`). branch := parentBranch + token (appended verbatim, NO separator inserted — the user's leading dash is the separator). This REPLACES auto-numbering.
- NAME-04 (custom collision = hard error): if the custom-token branch already exists, return a clear error (e.g. `branch %q already exists` naming the derived branch) — NO rename, NO auto-bump. This is a SEPARATE code path from today's generic "pass a different --branch" message and from the auto-vNNN skip loop.
- TOKEN PLUMBING (verified): cobra flag-parses a bare leading-dash arg (`wt new -patch01` → "unknown shorthand flag: 'p'"). The token reaches args intact via the `--` separator: `wt new -- -patch01` → args[0] == "-patch01". Keep CLI dumb for the positional case: positional → opts.Name (existing behavior); `Manager.Add` reinterprets opts.Name as the literal suffix token ONLY when derive mode is active AND opts.FromTemplate==false (empty opts.Name in derive mode → auto -vNNN). The ONLY new CLI responsibility is setting opts.FromTemplate=true in the --template case. Do NOT add a dedicated Suffix field. Document the `--` requirement in the command help/long text.
- CONSTRAINTS (CLAUDE.md): extend Manager.Add / AddOptions / GitRunner — do not bypass; no main-root regression; unit-testable via injected fakes (no real git in unit tests); derived names pass CheckRefFormat + collision checks before creation.
</design_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AddOptions.FromTemplate field + derive-mode detection/resolution in Manager.Add</name>
  <files>pkg/worktree/types.go, pkg/worktree/manager.go, pkg/worktree/manager_test.go, pkg/worktree/fakes_test.go</files>
  <read_first>
    - pkg/worktree/types.go (AddOptions struct)
    - pkg/worktree/manager.go (Add, resolveNames, effectivePrefix, worktreePath, containerPath)
    - pkg/worktree/interfaces.go (GitRunner, GitWorktree)
    - pkg/worktree/fakes_test.go (fakeGit: branches map, worktrees slice, BranchExists, ListWorktrees)
    - pkg/worktree/manager_test.go (newTestManager harness, existing Add/resolveNames tests)
  </read_first>
  <behavior>
    - Test (DETECT-01 + NAME-01): dir inside a non-main worktree (fakeGit.worktrees has an entry Path=worktree dir, Branch="refs/heads/wt/feature-login"; MainRoot returns a path that is NOT a prefix of the worktree dir) → Add with empty token resolves branch "wt/feature-login-v001", baseRef "wt/feature-login", target under the main container mirroring the full branch.
    - Test (NAME-02 gap-fill): branches "wt/feature-login-v001" and "wt/feature-login-v002" exist → empty token resolves "wt/feature-login-v003"; with only -v002 existing → resolves "wt/feature-login-v001" (lowest free, fills gap).
    - Test (NAME-03 custom token): token "-patch01" (passed as opts.Name, FromTemplate=false) → branch "wt/feature-login-patch01", baseRef "wt/feature-login".
    - Test (NAME-04 collision): token "-patch01" where "wt/feature-login-patch01" already exists → Add returns a non-nil error naming that branch; no AddWorktree call recorded.
    - Test (DERIVE-03): derive mode with opts.NoPrefix=true AND opts.PrefixOverride="x/" → branch is still "wt/feature-login-v001" (inherited prefix unchanged, overrides ignored). opts.BaseRef set → ignored, baseRef stays the parent branch.
    - Test (DETECT-02 regression, main root): dir == MainRoot (the matched worktree entry is the main one) → existing behavior; empty opts → generated name with wt/ prefix off cfg.BaseRef() (assert no -vNNN suffix).
    - Test (template fall-through): inside a worktree with opts.Name="feat/123" AND opts.FromTemplate=true → derive mode NOT triggered; today's resolveNames path runs (branch "wt/feat/123", baseRef cfg.BaseRef()), NO -vNNN, NO suffix mangling.
    - Test (explicit-branch fall-through): inside a worktree but opts.Branch != "" (or opts.FromBranch != "") → derive mode NOT triggered, today's path runs.
  </behavior>
  <action>
    Add field `FromTemplate bool` to AddOptions in types.go with a doc comment (set by the CLI when Name came from --template; suppresses derive-mode suffix reinterpretation). In Manager.Add, before the existing fromExisting/resolveNames fork, compute derive mode. Add an unexported helper `currentWorktreeBranch(dir, repoRoot string) (parentBranch string, ok bool)`: call m.git.ListWorktrees(dir), select the entry whose Path is the longest path-separator-bounded prefix of dir, return ok=false if that entry's Path == repoRoot or its Branch is empty/detached; otherwise strip the "refs/heads/" prefix and return ok=true. Gate derive mode on: ok==true AND opts.Branch=="" AND opts.FromBranch=="" AND !opts.FromTemplate. When derive mode is active, do NOT call resolveNames/effectivePrefix: if opts.Name=="" compute the auto suffix via an unexported `nextFreeVersion(repoRoot, parentBranch string) string` that loops n:=1.. building fmt.Sprintf("%s-v%03d", parentBranch, n) and stops at the first !m.git.BranchExists(repoRoot, candidate); otherwise treat opts.Name as the literal token and set branch = parentBranch + opts.Name. Set baseRef = parentBranch (ignore opts.BaseRef/cfg.BaseRef in derive mode). Set name = the derived branch with the inherited prefix trimmed for display (TrimPrefix against m.cfg.BranchPrefix(), best-effort). Keep the existing CheckRefFormat gate applied to the derived branch. For collisions: the auto path avoids existing branches (the v-loop skips them); for the custom-token path ensure derive+custom collisions return an error naming the derived branch (the message must name the branch, distinct from today's "pass a different --branch"). Leave the main-root code path byte-for-byte equivalent to today. fakeGit already has worktrees + a branches map driving ListWorktrees/BranchExists — extend only if a test needs more.
  </action>
  <verify>
    <automated>go test ./pkg/worktree/ -run 'Add|Derive|Version|Name|Resolve|Template' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./pkg/worktree/ -count=1` exits 0 (new derive tests + all pre-existing tests pass — no regression).
    - AddOptions has a FromTemplate bool field; manager.go contains `currentWorktreeBranch` and a lowest-free `-vNNN` search; main-root Add path is unchanged (existing TestAdd*/TestResolveNames* still pass).
    - A unit test exercises detection where MainRoot is NOT a prefix of the worktree dir (detection is genuinely tested, not short-circuited).
    - A unit test proves `--template` inside a worktree (Name set, FromTemplate=true) falls through to today's behavior with no `-vNNN` and no suffix mangling.
  </acceptance_criteria>
  <done>Derive-mode detection, auto `-vNNN`, custom-token suffix, collision error, prefix-inheritance, template/explicit-branch fall-through, and main-root non-regression are all covered by passing unit tests using the injected fakes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI positional-token passthrough + FromTemplate signal + help text in cmd/wt/new.go</name>
  <files>cmd/wt/new.go, cmd/wt/new_test.go</files>
  <read_first>
    - cmd/wt/new.go (buildAddOptions: the `case tmpl != ""` branch that sets opts.Name from ResolveTemplate, and the default positional branch that sets opts.Name = args[0])
    - cmd/wt/new_test.go (existing buildAddOptions table tests + fake addResolver)
  </read_first>
  <behavior>
    - Test (token passthrough): buildAddOptions(..., args=["-patch01"], no tmpl/from-branch/branch) → opts.Name == "-patch01", opts.FromTemplate == false.
    - Test (template sets signal): buildAddOptions(..., tmpl="feature", args=["ticket:123"]) → opts.Name == rendered name AND opts.FromTemplate == true.
    - Test (regression): args=["myname"] → opts.Name == "myname", FromTemplate false, exactly as today; >1 positional still errors; --from-branch + positional still errors.
  </behavior>
  <action>
    In buildAddOptions, set opts.FromTemplate = true in the `case tmpl != ""` branch (right where opts.Name is assigned from ResolveTemplate). Keep the default positional case dumb: opts.Name = args[0] with the leading-dash token surviving unchanged (no trimming, no validation; the domain interprets it). Do NOT add a Suffix field or mode detection to the CLI. Update newCmd.Use/Short and add a Long description documenting that, inside a worktree, `wt new` derives `<current-branch>-vNNN` from the current branch, and that a custom suffix token starting with `-` must be passed after `--` (e.g. `wt new -- -patch01`) because cobra otherwise parses a leading-dash arg as a flag. Do not change flag definitions. Extend new_test.go with the three behaviors above.
  </action>
  <verify>
    <automated>go test ./cmd/wt/ -run 'BuildAddOptions|New' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wt/ -count=1` exits 0.
    - new_test.go asserts: args ["-patch01"] → opts.Name=="-patch01" & FromTemplate==false; --template path → FromTemplate==true.
    - newCmd Long text documents the `wt new -- -patch01` form for custom suffixes.
  </acceptance_criteria>
  <done>The CLI passes a leading-dash positional token to opts.Name intact, marks template-derived names via FromTemplate, documents the `--` form, and preserves all existing arg-parsing behavior.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration test covering all five success criteria</name>
  <files>pkg/worktree/manager_integration_test.go</files>
  <read_first>
    - pkg/worktree/manager_integration_test.go (existing real-git harness: temp repo init, Manager construction, Add/Remove e2e patterns, any build tag)
    - internal/git/git.go, internal/git/worktree.go (AddWorktree, ListWorktrees real impls)
    - pkg/worktree/manager.go (final Add behavior from Task 1)
  </read_first>
  <action>
    Add integration tests using the existing real-git harness (match its build tag exactly if it has one). Set up a real temp repo with at least one commit, create an initial managed worktree on a branch (e.g. via Manager.Add from root), then run Manager.Add from INSIDE that worktree's directory. Assert the five ROADMAP success criteria end-to-end: (1) empty token from inside a worktree → `<branch>-v001`, branch cut from the parent branch's tip, worktree dir under the main container; (2) repeated Add picks lowest-free `-vNNN`, skipping existing and filling a manually-created gap; (3) custom token `-patch01` (opts.Name="-patch01", FromTemplate=false) → `<branch>-patch01`, and a second Add with the same token returns an error (no second worktree created); (4) `--no-prefix`/PrefixOverride in derive mode leaves the inherited prefix intact; (5) Add from the main repo root still branches off base_ref/HEAD with unchanged naming/placement. Use real git rev-parse to confirm the new branch's start commit equals the parent branch tip for criterion (1)/DERIVE-01.
  </action>
  <verify>
    <automated>go test ./pkg/worktree/ -run Integration -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The integration test file compiles and `go test ./pkg/worktree/ -run Integration -count=1` exits 0 against real git.
    - All five ROADMAP success criteria are asserted (derive v001, gap-filling vNNN, custom token + collision error, prefix verbatim, main-root unchanged).
    - DERIVE-01 is checked by comparing the new branch's start commit to the parent branch tip via real git.
  </acceptance_criteria>
  <done>A real-git integration test proves all five success criteria pass end-to-end, anchoring the vertical slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → generated git ref + filesystem path | The custom suffix token and the derived parent branch name flow into a new branch ref AND (via worktreePath) into an on-disk directory path under the container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Custom suffix token → derived branch ref (manager.go Add) | mitigate | The derived branch passes the existing `m.git.CheckRefFormat(branch)` gate before any creation; an illegal token (e.g. embedded `..`, control chars, leading/trailing junk) is rejected. Keep this gate on the derive path. |
| T-01-02 | Tampering | Derived branch → worktreePath directory (path traversal) | mitigate | worktreePath mirrors the branch under the container via filepath.FromSlash; CheckRefFormat already forbids the ref forms (`..`, leading `/`, etc.) that would escape the container, so no traversal is reachable from a ref that passed the gate. No separate path sanitization needed. |
| T-01-03 | Spoofing/Detection error | Worktree-vs-main detection (currentWorktreeBranch) | accept | Detection compares the listed worktree Path against MainRoot; container is a sibling of repoRoot so a false main↔worktree match is not reachable. Local single-user CLI, low-value target — residual risk accepted. |
| T-01-SC | Tampering | Dependency installs | accept | No new third-party dependencies are added in this phase (Go stdlib + existing cobra only); no package-legitimacy gate required. |

Risk profile is proportionately LOW: a local single-user CLI shelling out to the user's own git, no network, no new dependencies. The single load-bearing control is the existing `CheckRefFormat` gate on the derived branch.
</threat_model>

<verification>
- `go build ./...` succeeds.
- `go test ./... -count=1` exits 0 (unit + integration).
- `go vet ./...` clean; `gofmt -l pkg/worktree cmd/wt` prints nothing.
- Manual smoke (optional): from inside a managed worktree on branch `wt/feature-login`, `wt new` creates `wt/feature-login-v001`; `wt new -- -patch01` creates `wt/feature-login-patch01`; from repo root `wt new` behaves as today.
</verification>

<success_criteria>
All five ROADMAP Phase 1 success criteria hold, proven by unit tests (Task 1/2) and the real-git integration test (Task 3):
1. `wt new` (no token) inside a worktree → `<branch>-v001`, cut from the committed tip, in the main container.
2. Repeated `wt new` → lowest free `-vNNN`, skipping existing, filling gaps.
3. `wt new -- "-patch01"` → `<branch>-patch01`; existing → clear collision error, no rename/auto-bump.
4. Derived branch keeps the parent prefix verbatim; `--no-prefix`/`--branch-prefix` have no effect in derive mode.
5. `wt new` from the main repo root is unchanged (branches off base_ref/HEAD, same naming/placement).
</success_criteria>

<output>
Create `.planning/phases/01-worktree-derived-new/01-01-SUMMARY.md` when done.
</output>
